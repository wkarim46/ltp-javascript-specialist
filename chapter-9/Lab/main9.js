var window;

window.onload = function() {
	/*var sentence = "its my way or no way.";

	function capWord(str) {
		return str.charAt(0).toUpperCase() + str.slice(1);

	}
	alert(capWord (sentence));
};*/
	 var str = "I Am A Bad Man";
	 var res = str.split(" ");
	 res.sort();
	 console.log(res);
};
